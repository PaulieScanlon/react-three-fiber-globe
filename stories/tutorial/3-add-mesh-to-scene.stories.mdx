import { Meta } from '@storybook/addon-docs';
import { Canvas } from '@react-three/fiber';
import { OrbitControls } from '@react-three/drei';

import SceneWrapper from './scene-wrapper';

<Meta title="Tutorial/3. Add mesh to scene" />

# Add Mesh to Scene

<SceneWrapper>
  <Canvas
    camera={{
      fov: 75,
      position: [0, 0, 2.1]
    }}
    style={{
      cursor: 'move'
    }}
  >
    <OrbitControls enableRotate={true} enableZoom={false} enablePan={false} />
    <ambientLight intensity={1.3} />
    <pointLight position={[-10, -10, -10]} intensity={0.4} />
    <mesh>
      <sphereGeometry args={[1, 32]} />
      <meshPhongMaterial color="#191919" transparent={true} opacity={0.8} />
    </mesh>
  </Canvas>
</SceneWrapper>

1. Import `<ThreeMesh />` and add to scene.

```diff
// three-scene.js

import React from 'react';
import { Canvas } from '@react-three/fiber';

+ import ThreeMesh from './three-mesh'


const ThreeScene = () => {
  return (
    <Canvas
      camera={{
        fov: 75,
        position: [0, 0, 2.1]
      }}
      style={{
        cursor: 'move'
      }}
    >
    <OrbitControls enableRotate={true} enableZoom={true} enablePan={false} />
    <ambientLight intensity={1.3} />
    <pointLight position={[-10, -10, -10]} intensity={0.4} />
+     <ThreeMesh>
    </Canvas>
  );
};

export default ThreeScene;
```
