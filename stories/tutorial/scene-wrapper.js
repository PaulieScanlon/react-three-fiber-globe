import React from 'react';

const SceneWrapper = ({ children }) => {
  return <div style={{ margin: '48px 0', height: 500, border: '1px solid #232323', borderRadius: 8 }}>{children}</div>;
};

export default SceneWrapper;
