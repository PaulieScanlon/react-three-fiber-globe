import { Meta } from '@storybook/addon-docs';

<Meta title="Tutorial/4. Create graticules" />

# Create Graticules

1. Install dependencies

```shell
npm install three-geojson-geometry d3-geo
```

2. Create new component called `<ThreeGraticule />`.
3. Add `<group>`, `<lineSegments>`, and `<lineBasicMaterial />`.

```javascript
// three-graticule.js

import React from 'react';
import { GeoJsonGeometry } from 'three-geojson-geometry';
import { geoGraticule10 } from 'd3-geo';

const ThreeGraticule = () => {
  return (
    <group>
      <lineSegments geometry={new GeoJsonGeometry(geoGraticule10(), 1)}>
        <lineBasicMaterial color="#3c3c3c" />
      </lineSegments>
    </group>
  );
};

export default ThreeGraticule;
```

## Reference Links

- [https://github.com/d3/d3-geo#geoGraticule10](https://github.com/d3/d3-geo#geoGraticule10)
